<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="36dp"
    android:layout_marginBottom="2dp"
    android:descendantFocusability="afterDescendants"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <!-- Schlanker Senderkopf mit striktem Fokus-Einschluss -->
    <LinearLayout
        android:id="@+id/channelHeader"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:background="@drawable/card_focus_selector"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp"
        android:nextFocusLeft="@+id/channelHeader"
        android:nextFocusRight="@+id/recyclerChannelPrograms"
        android:layout_marginEnd="4dp">

        <TextView
            android:id="@+id/txtChannelNum"
            android:layout_width="26dp"
            android:layout_height="wrap_content"
            android:text="1"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/imgChannelLogo"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_marginEnd="6dp"
            android:src="@drawable/tv_banner" />

        <TextView
            android:id="@+id/txtChannelName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="marquee"
            android:singleLine="true"
            android:text="Sendername"
            android:textColor="@color/text_primary"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Rechter horizontaler EPG Timeline Zeitstrahl -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerChannelPrograms"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="horizontal" />

</LinearLayout>
